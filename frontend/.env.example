# ── 开发环境（.env）────────────────────────────────────────────
VITE_API_BASE=http://localhost:8000
VITE_BASE=/

# ── 生产环境（.env.production，已提交）──────────────────────────
# VITE_API_BASE=          # 留空 = 同源，由 nginx 路由到后端
# VITE_BASE=/blog/        # 博客挂载在 /blog/ 子路径
